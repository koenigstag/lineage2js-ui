{
  "name": "lineage2js-ui",
  "version": "0.0.1",
  "private": true,
  "description": "Monorepo for Lineage 2 JS network protocol library and web UI client",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/koenigstag/lineage2js-ui"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
