PORT=4000
# Binds to loopback only by default -- put a reverse proxy (nginx/Caddy) in
# front for TLS/external access rather than widening this to 0.0.0.0.
HOST=127.0.0.1
ASSETS_DIR=./assets
MAX_AGE_SECONDS=3600
